import { PortalShell } from '@/app/components/PortalShell';
import { Badge } from '@/app/components/Badge';
export function PortalListPage({ role, title, eyebrow, items }: { role: 'technician' | 'subscriber'; title: string; eyebrow: string; items: { title: string; detail: string; status: string }[] }) { return <PortalShell role={role}><p className="text-sm font-bold uppercase tracking-widest text-[#3b4fd8]">{eyebrow}</p><h1 className="mt-2 text-3xl font-bold tracking-tight">{title}</h1><div className="mt-8 grid gap-4">{items.map((item) => <article className="flex flex-wrap items-center justify-between gap-4 rounded-xl border border-slate-200 bg-white p-5 transition-colors duration-100 hover:bg-[#f8fafc]" key={item.title}><div><h2 className="font-bold">{item.title}</h2><p className="mt-1 text-sm text-slate-500">{item.detail}</p></div><Badge value={item.status} /></article>)}</div></PortalShell>; }
