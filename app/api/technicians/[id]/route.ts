import { NextResponse } from 'next/server';
import { prisma } from '@/app/lib/prisma';
type Context = { params: Promise<{ id: string }> };
export async function PUT(request: Request, context: Context) { try { const { id } = await context.params; const body = await request.json(); const technician = await prisma.technician.update({ where: { id }, data: { name: body.name, email: body.email, contact: body.contact || null, specialization: body.specialization, status: body.status?.replace(' ', '_'), archived: typeof body.archived === 'boolean' ? body.archived : undefined } }); return NextResponse.json({ data: technician, error: null }); } catch (error) { return NextResponse.json({ data: null, error: error instanceof Error ? error.message : 'Unable to update technician.' }, { status: 500 }); } }
export async function DELETE(_: Request, context: Context) { const { id } = await context.params; await prisma.$transaction([prisma.installation.deleteMany({ where: { technicianId: id } }), prisma.user.deleteMany({ where: { technicianId: id } }), prisma.technician.delete({ where: { id } })]); return new NextResponse(null, { status: 204 }); }
